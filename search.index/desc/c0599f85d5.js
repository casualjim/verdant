rd_("De(De)serialized as <code>b</code>, <strong>not</strong> <code>b</code>0De(De)serialized as <code>c</code>, <strong>not</strong> <code>c</code>0E`Provides the <code>c</code> language, supports these file types: <code>c</code>.00De(De)serialized as <code>d</code>, <strong>not</strong> <code>d</code>0E`Provides the <code>d</code> language, supports these file types: <code>d</code>.B`(De)serialized as <code>e</code>0B`(De)serialized as <code>j</code>0B`(De)serialized as <code>q</code>0B`(De)serialized as <code>r</code>0E`Provides the <code>r</code> language, supports these file types: <code>r</code>.De(De)serialized as <code>v</code>, <strong>not</strong> <code>v</code>0E`Provides the <code>v</code> language, supports these file types: <code>v</code>.AeGet the parser for c.00Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AeGet the parser for d.11AeGet the parser for r.22AeGet the parser for v.3Ba(De)serialized as <code>bc</code>0Dg(De)serialized as <code>be</code>, <strong>not</strong> <code>be</code>0Ba(De)serialized as <code>bp</code>0EbProvides the <code>bp</code> language, supports these file types: <code>bp</code>.Ba(De)serialized as <code>c3</code>0EbProvides the <code>c3</code> language, supports these file types: <code>c3</code>.Dg(De)serialized as <code>cf</code>, <strong>not</strong> <code>cf</code>0Ba(De)serialized as <code>ch</code>0Ba(De)serialized as <code>cl</code>0Ba(De)serialized as <code>eq</code>0Dg(De)serialized as <code>gf</code>, <strong>not</strong> <code>gf</code>0Ba(De)serialized as <code>gn</code>0EbProvides the <code>gn</code> language, supports these file types: <code>gn</code>.Dg(De)serialized as <code>go</code>, <strong>not</strong> <code>go</code>0EbProvides the <code>go</code> language, supports these file types: <code>go</code>.00Dg(De)serialized as <code>hb</code>, <strong>not</strong> <code>hb</code>0Dg(De)serialized as <code>hy</code>, <strong>not</strong> <code>hy</code>0Dg(De)serialized as <code>i7</code>, <strong>not</strong> <code>i7</code>0Ba(De)serialized as <code>io</code>0Dg(De)serialized as <code>jq</code>, <strong>not</strong> <code>jq</code>0EbProvides the <code>jq</code> language, supports these file types: <code>jq</code>.Dg(De)serialized as <code>ld</code>, <strong>not</strong> <code>ld</code>0EbProvides the <code>ld</code> language, supports these file types: <code>ld</code>.Ba(De)serialized as <code>lf</code>0Ba(De)serialized as <code>m4</code>0Ba(De)serialized as <code>mf</code>0Ba(De)serialized as <code>mp</code>0Ba(De)serialized as <code>nl</code>0Ba(De)serialized as <code>nq</code>0Dg(De)serialized as <code>nu</code>, <strong>not</strong> <code>nu</code>0EbProvides the <code>nu</code> language, supports these file types: <code>nu</code>.AjContains the success value000Ba(De)serialized as <code>ox</code>0Ba(De)serialized as <code>oz</code>0Ba(De)serialized as <code>p4</code>0Ba(De)serialized as <code>pf</code>0Dg(De)serialized as <code>po</code>, <strong>not</strong> <code>po</code>0EbProvides the <code>po</code> language, supports these file types: <code>po</code>.Ba(De)serialized as <code>pq</code>0Dg(De)serialized as <code>ql</code>, <strong>not</strong> <code>ql</code>0EbProvides the <code>ql</code> language, supports these file types: <code>ql</code>.00Ba(De)serialized as <code>rc</code>0Ba(De)serialized as <code>sd</code>0Dg(De)serialized as <code>sh</code>, <strong>not</strong> <code>sh</code>0Ba(De)serialized as <code>sm</code>0Dg(De)serialized as <code>st</code>, <strong>not</strong> <code>st</code>0Ba(De)serialized as <code>tf</code>0Dg(De)serialized as <code>uc</code>, <strong>not</strong> <code>uc</code>0Dg(De)serialized as <code>vb</code>, <strong>not</strong> <code>vb</code>0Ba(De)serialized as <code>xc</code>0Ba(De)serialized as <code>xs</code>0CnGet this <code>Style</code>\xe2\x80\x99s background <code>Color</code>.CkGet the default background color, if the theme defines one.CnThe background color to use for this style, specified as a \xe2\x80\xa6210Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AfGet the parser for bp.11AfGet the parser for c3.2CkGet the default foreground color, if the theme defines one.03AfGet the parser for gn.4AfGet the parser for go.00BnGet a numeric id for this node that is unique.6AfGet the parser for jq.77AfGet the parser for ld.88AfGet the parser for nu.99AfGet the parser for po.:AfGet the parser for ql.00Bb(De)serialized as <code>a65</code>0Bb(De)serialized as <code>aap</code>0Bb(De)serialized as <code>abc</code>0Di(De)serialized as <code>ada</code>, <strong>not</strong> <code>ada</code>0EdProvides the <code>ada</code> language, supports these file types: <code>ada</code>.Di(De)serialized as <code>ags</code>, <strong>not</strong> <code>ags</code>0Bb(De)serialized as <code>aml</code>0Di(De)serialized as <code>ant</code>, <strong>not</strong> <code>ant</code>0BeMatch any character (<code>.</code>).n<code>.</code>Bb(De)serialized as <code>apl</code>0Bb(De)serialized as <code>arc</code>0Bb(De)serialized as <code>art</code>0Bb(De)serialized as <code>asl</code>0Bb(De)serialized as <code>asm</code>0EhProvides the <code>asm</code> language, supports these file types: <code>asm</code>, \xe2\x80\xa600Di(De)serialized as <code>asn</code>, <strong>not</strong> <code>asn</code>0Di(De)serialized as <code>asp</code>, <strong>not</strong> <code>asp</code>0Di(De)serialized as <code>asy</code>, <strong>not</strong> <code>asy</code>0Bb(De)serialized as <code>ave</code>0Bb(De)serialized as <code>awk</code>0EdProvides the <code>awk</code> language, supports these file types: <code>awk</code>.Bb(De)serialized as <code>bat</code>0Di(De)serialized as <code>bdf</code>, <strong>not</strong> <code>bdf</code>0Di(De)serialized as <code>bib</code>, <strong>not</strong> <code>bib</code>0EdProvides the <code>bib</code> language, supports these file types: <code>bib</code>.Bb(De)serialized as <code>boo</code>0Bb(De)serialized as <code>bqn</code>0Di(De)serialized as <code>bro</code>, <strong>not</strong> <code>bro</code>0Bb(De)serialized as <code>bru</code>0Di(De)serialized as <code>bst</code>, <strong>not</strong> <code>bst</code>0Bb(De)serialized as <code>btm</code>0Di(De)serialized as <code>bzl</code>, <strong>not</strong> <code>bzl</code>0EdProvides the <code>bzl</code> language, supports these file types: <code>bzl</code>.Bb(De)serialized as <code>bzr</code>0Di(De)serialized as <code>cdc</code>, <strong>not</strong> <code>cdc</code>0Bb(De)serialized as <code>cdl</code>0Di(De)serialized as <code>cds</code>, <strong>not</strong> <code>cds</code>0Bb(De)serialized as <code>cfg</code>0Bb(De)serialized as <code>cil</code>0Di(De)serialized as <code>coq</code>, <strong>not</strong> <code>coq</code>0Bb(De)serialized as <code>cpp</code>0EdProvides the <code>cpp</code> language, supports these file types: <code>cpp</code>.00Bb(De)serialized as <code>crm</code>0Di(De)serialized as <code>csc</code>, <strong>not</strong> <code>csc</code>0Bb(De)serialized as <code>csh</code>0Bb(De)serialized as <code>csp</code>0Bb(De)serialized as <code>css</code>0EdProvides the <code>css</code> language, supports these file types: <code>css</code>.00Bb(De)serialized as <code>csv</code>0EdProvides the <code>csv</code> language, supports these file types: <code>csv</code>.Di(De)serialized as <code>cue</code>, <strong>not</strong> <code>cue</code>0EdProvides the <code>cue</code> language, supports these file types: <code>cue</code>.Bb(De)serialized as <code>cvs</code>0Di(De)serialized as <code>cwl</code>, <strong>not</strong> <code>cwl</code>0Bb(De)serialized as <code>dax</code>0Bb(De)serialized as <code>dcd</code>0Di(De)serialized as <code>dcl</code>, <strong>not</strong> <code>dcl</code>0Bb(De)serialized as <code>def</code>0Di(De)serialized as <code>dot</code>, <strong>not</strong> <code>dot</code>0EdProvides the <code>dot</code> language, supports these file types: <code>dot</code>.Bb(De)serialized as <code>dsl</code>0Bb(De)serialized as <code>dtd</code>0EdProvides the <code>dtd</code> language, supports these file types: <code>dtd</code>.Di(De)serialized as <code>dts</code>, <strong>not</strong> <code>dts</code>0EdProvides the <code>dts</code> language, supports these file types: <code>dts</code>.Bb(De)serialized as <code>ecd</code>0Bb(De)serialized as <code>ecl</code>0Di(De)serialized as <code>ecr</code>, <strong>not</strong> <code>ecr</code>0Bb(De)serialized as <code>edn</code>0Bb(De)serialized as <code>eds</code>0EdProvides the <code>eds</code> language, supports these file types: <code>eds</code>.DfProvides the <code>ejs</code> language, supports these file types: \xe2\x80\xa600Bb(De)serialized as <code>elf</code>0Bb(De)serialized as <code>elm</code>0EdProvides the <code>elm</code> language, supports these file types: <code>elm</code>.BmMatch the end of the string (<code>$</code>).n<code>$</code>kEnd of fileEhProvides the <code>erb</code> language, supports these file types: <code>eruby</code>\xe2\x80\xa600AhContains the error value000Di(De)serialized as <code>fan</code>, <strong>not</strong> <code>fan</code>0Bb(De)serialized as <code>fga</code>0Di(De)serialized as <code>fgl</code>, <strong>not</strong> <code>fgl</code>0EdProvides the <code>fsd</code> language, supports these file types: <code>fsd</code>.Bb(De)serialized as <code>fsh</code>0EdProvides the <code>fsh</code> language, supports these file types: <code>fsh</code>.Bb(De)serialized as <code>gap</code>0EdProvides the <code>gap</code> language, supports these file types: <code>gap</code>.Di(De)serialized as <code>gas</code>, <strong>not</strong> <code>gas</code>0Bb(De)serialized as <code>gdb</code>0Bb(De)serialized as <code>gel</code>0Bb(De)serialized as <code>git</code>0Bb(De)serialized as <code>gpg</code>0EdProvides the <code>gpg</code> language, supports these file types: <code>gpg</code>.Di(De)serialized as <code>gsp</code>, <strong>not</strong> <code>gsp</code>0Bb(De)serialized as <code>gyp</code>0Di(De)serialized as <code>hcl</code>, <strong>not</strong> <code>hcl</code>0EdProvides the <code>hcl</code> language, supports these file types: <code>hcl</code>.Bb(De)serialized as <code>hex</code>0Bb(De)serialized as <code>hip</code>0Bb(De)serialized as <code>hog</code>0Bb(De)serialized as <code>idl</code>0EdProvides the <code>idl</code> language, supports these file types: <code>idl</code>.Di(De)serialized as <code>ijm</code>, <strong>not</strong> <code>ijm</code>0EhProvides the <code>ini</code> language, supports these file types: <code>ini</code>, \xe2\x80\xa6Bb(De)serialized as <code>ink</code>0Di(De)serialized as <code>irc</code>, <strong>not</strong> <code>irc</code>0Di(De)serialized as <code>iss</code>, <strong>not</strong> <code>iss</code>0Bb(De)serialized as <code>ist</code>0Bb(De)serialized as <code>jac</code>0Bb(De)serialized as <code>jai</code>0Bb(De)serialized as <code>jal</code>0Bb(De)serialized as <code>jam</code>0Bb(De)serialized as <code>jcl</code>0Di(De)serialized as <code>jsp</code>, <strong>not</strong> <code>jsp</code>0Di(De)serialized as <code>jsx</code>, <strong>not</strong> <code>jsx</code>0Di(De)serialized as <code>jte</code>, <strong>not</strong> <code>jte</code>0Di(De)serialized as <code>kak</code>, <strong>not</strong> <code>kak</code>0Bb(De)serialized as <code>kcl</code>0EdProvides the <code>kcl</code> language, supports these file types: <code>kcl</code>.Bb(De)serialized as <code>kdl</code>0EdProvides the <code>kdl</code> language, supports these file types: <code>kdl</code>.Bb(De)serialized as <code>kit</code>0Bb(De)serialized as <code>kix</code>0Bb(De)serialized as <code>kos</code>0EdProvides the <code>kos</code> language, supports these file types: <code>kos</code>.Bb(De)serialized as <code>krl</code>0Bb(De)serialized as <code>ksh</code>0Di(De)serialized as <code>ksy</code>, <strong>not</strong> <code>ksy</code>0Di(De)serialized as <code>kwt</code>, <strong>not</strong> <code>kwt</code>0Bb(De)serialized as <code>leo</code>0EdProvides the <code>leo</code> language, supports these file types: <code>leo</code>.Di(De)serialized as <code>lex</code>, <strong>not</strong> <code>lex</code>0Bb(De)serialized as <code>lfe</code>0Bb(De)serialized as <code>lpc</code>0Bb(De)serialized as <code>lsl</code>0Bb(De)serialized as <code>lss</code>0Bb(De)serialized as <code>lua</code>0EdProvides the <code>lua</code> language, supports these file types: <code>lua</code>.00Bb(De)serialized as <code>map</code>0Bb(De)serialized as <code>mdx</code>0Bb(De)serialized as <code>mel</code>0Bb(De)serialized as <code>mgl</code>0Bb(De)serialized as <code>mgp</code>0Bb(De)serialized as <code>mib</code>0Bb(De)serialized as <code>mix</code>0Di(De)serialized as <code>mma</code>, <strong>not</strong> <code>mma</code>0Bb(De)serialized as <code>mmp</code>0Di(De)serialized as <code>moo</code>, <strong>not</strong> <code>moo</code>0Di(De)serialized as <code>mss</code>, <strong>not</strong> <code>mss</code>0Bb(De)serialized as <code>muf</code>0Bb(De)serialized as <code>ncf</code>0Bb(De)serialized as <code>nim</code>0EdProvides the <code>nim</code> language, supports these file types: <code>nim</code>.Bb(De)serialized as <code>nit</code>0Di(De)serialized as <code>nix</code>, <strong>not</strong> <code>nix</code>0EdProvides the <code>nix</code> language, supports these file types: <code>nix</code>.00Di(De)serialized as <code>njk</code>, <strong>not</strong> <code>njk</code>0Bb(De)serialized as <code>nqc</code>0EdProvides the <code>nqc</code> language, supports these file types: <code>nqc</code>.Di(De)serialized as <code>obj</code>, <strong>not</strong> <code>obj</code>0Bb(De)serialized as <code>ooc</code>0Bb(De)serialized as <code>opa</code>0Bb(De)serialized as <code>opl</code>0Bb(De)serialized as <code>ora</code>0Bb(De)serialized as <code>org</code>0Bb(De)serialized as <code>pan</code>0Bb(De)serialized as <code>pdf</code>0Bb(De)serialized as <code>pem</code>0EdProvides the <code>pem</code> language, supports these file types: <code>pem</code>.Bb(De)serialized as <code>pgn</code>0EdProvides the <code>pgn</code> language, supports these file types: <code>pgn</code>.Di(De)serialized as <code>php</code>, <strong>not</strong> <code>php</code>0EdProvides the <code>php</code> language, supports these file types: <code>php</code>.00Di(De)serialized as <code>pir</code>, <strong>not</strong> <code>pir</code>0Di(De)serialized as <code>pkl</code>, <strong>not</strong> <code>pkl</code>0EdProvides the <code>pkl</code> language, supports these file types: <code>pkl</code>.Bb(De)serialized as <code>pli</code>0Bb(De)serialized as <code>plm</code>0Bb(De)serialized as <code>plp</code>0Di(De)serialized as <code>pod</code>, <strong>not</strong> <code>pod</code>0EdProvides the <code>pod</code> language, supports these file types: <code>pod</code>.Di(De)serialized as <code>pov</code>, <strong>not</strong> <code>pov</code>0Bb(De)serialized as <code>ppd</code>0Di(De)serialized as <code>ps1</code>, <strong>not</strong> <code>ps1</code>0EdProvides the <code>ps1</code> language, supports these file types: <code>ps1</code>.Bb(De)serialized as <code>psf</code>0Bb(De)serialized as <code>psl</code>0Bb(De)serialized as <code>psv</code>0EdProvides the <code>psv</code> language, supports these file types: <code>psv</code>.Bb(De)serialized as <code>ptx</code>0Bb(De)serialized as <code>pug</code>0EdProvides the <code>pug</code> language, supports these file types: <code>pug</code>.EdProvides the <code>qml</code> language, supports these file types: <code>qml</code>.Di(De)serialized as <code>raw</code>, <strong>not</strong> <code>raw</code>0Bb(De)serialized as <code>rbs</code>0EdProvides the <code>rbs</code> language, supports these file types: <code>rbs</code>.Bb(De)serialized as <code>rcs</code>0Bb(De)serialized as <code>rez</code>0Bb(De)serialized as <code>rib</code>0Bb(De)serialized as <code>rmd</code>0Bb(De)serialized as <code>rnc</code>0Bb(De)serialized as <code>rng</code>0Bb(De)serialized as <code>roc</code>0EdProvides the <code>roc</code> language, supports these file types: <code>roc</code>.Bb(De)serialized as <code>ron</code>0EdProvides the <code>ron</code> language, supports these file types: <code>ron</code>.Bb(De)serialized as <code>rpl</code>0Di(De)serialized as <code>rst</code>, <strong>not</strong> <code>rst</code>0EdProvides the <code>rst</code> language, supports these file types: <code>rst</code>.Di(De)serialized as <code>rtf</code>, <strong>not</strong> <code>rtf</code>0Bb(De)serialized as <code>sas</code>0Bb(De)serialized as <code>sbt</code>0Bb(De)serialized as <code>sdc</code>0Bb(De)serialized as <code>sdl</code>0Bb(De)serialized as <code>sed</code>0FdA set of <code>SetPatternObject</code>s (eg. <code>[a-z_%u]</code>, <code>[^a-z_%u]</code>), the \xe2\x80\xa6Di(De)serialized as <code>sfv</code>, <strong>not</strong> <code>sfv</code>0Bb(De)serialized as <code>sil</code>0Di(De)serialized as <code>sml</code>, <strong>not</strong> <code>sml</code>0Di(De)serialized as <code>soy</code>, <strong>not</strong> <code>soy</code>0Bb(De)serialized as <code>sqf</code>0Bb(De)serialized as <code>sql</code>0EhProvides the <code>sql</code> language, supports these file types: <code>sql</code>, \xe2\x80\xa600Bb(De)serialized as <code>sqr</code>0Di(De)serialized as <code>srt</code>, <strong>not</strong> <code>srt</code>0Bb(De)serialized as <code>ssa</code>0Bb(De)serialized as <code>stp</code>0Bb(De)serialized as <code>svg</code>0Bb(De)serialized as <code>svn</code>0Bb(De)serialized as <code>tak</code>0Di(De)serialized as <code>tal</code>, <strong>not</strong> <code>tal</code>0EdProvides the <code>tal</code> language, supports these file types: <code>tal</code>.Di(De)serialized as <code>tcl</code>, <strong>not</strong> <code>tcl</code>0EdProvides the <code>tcl</code> language, supports these file types: <code>tcl</code>.Bb(De)serialized as <code>tea</code>0Di(De)serialized as <code>tex</code>, <strong>not</strong> <code>tex</code>0EdProvides the <code>tex</code> language, supports these file types: <code>tex</code>.Di(De)serialized as <code>tla</code>, <strong>not</strong> <code>tla</code>0EdProvides the <code>tla</code> language, supports these file types: <code>tla</code>.Bb(De)serialized as <code>tli</code>0Bb(De)serialized as <code>tpp</code>0Di(De)serialized as <code>tsv</code>, <strong>not</strong> <code>tsv</code>0EdProvides the <code>tsv</code> language, supports these file types: <code>tsv</code>.Di(De)serialized as <code>tsx</code>, <strong>not</strong> <code>tsx</code>0EdProvides the <code>tsx</code> language, supports these file types: <code>tsx</code>.00Bb(De)serialized as <code>txl</code>0Bb(De)serialized as <code>uil</code>0Bb(De)serialized as <code>uno</code>0Bb(De)serialized as <code>usd</code>0EdProvides the <code>usd</code> language, supports these file types: <code>usd</code>.Bb(De)serialized as <code>vcl</code>0Di(De)serialized as <code>vdf</code>, <strong>not</strong> <code>vdf</code>0Di(De)serialized as <code>vhs</code>, <strong>not</strong> <code>vhs</code>0Di(De)serialized as <code>vim</code>, <strong>not</strong> <code>vim</code>0EdProvides the <code>vim</code> language, supports these file types: <code>vim</code>.Bb(De)serialized as <code>vrl</code>0EdProvides the <code>vrl</code> language, supports these file types: <code>vrl</code>.Di(De)serialized as <code>vtl</code>, <strong>not</strong> <code>vtl</code>0EdProvides the <code>vto</code> language, supports these file types: <code>vto</code>.Di(De)serialized as <code>vtt</code>, <strong>not</strong> <code>vtt</code>0Bb(De)serialized as <code>vue</code>0EdProvides the <code>vue</code> language, supports these file types: <code>vue</code>.Bb(De)serialized as <code>wat</code>0EdProvides the <code>wat</code> language, supports these file types: <code>wat</code>.Di(De)serialized as <code>wdl</code>, <strong>not</strong> <code>wdl</code>0Bb(De)serialized as <code>web</code>0Di(De)serialized as <code>wit</code>, <strong>not</strong> <code>wit</code>0EdProvides the <code>wit</code> language, supports these file types: <code>wit</code>.Bb(De)serialized as <code>wml</code>0Bb(De)serialized as <code>wsh</code>0Bb(De)serialized as <code>xbl</code>0Di(De)serialized as <code>xbm</code>, <strong>not</strong> <code>xbm</code>0Di(De)serialized as <code>xml</code>, <strong>not</strong> <code>xml</code>0EdProvides the <code>xml</code> language, supports these file types: <code>xml</code>.Di(De)serialized as <code>xpm</code>, <strong>not</strong> <code>xpm</code>0Bb(De)serialized as <code>xsd</code>0Bb(De)serialized as <code>yul</code>0Bb(De)serialized as <code>z8a</code>0Bb(De)serialized as <code>zap</code>0Bb(De)serialized as <code>zig</code>0EdProvides the <code>zig</code> language, supports these file types: <code>zig</code>.00Bb(De)serialized as <code>zil</code>0Bb(De)serialized as <code>zir</code>0Bb(De)serialized as <code>zsh</code>0EdProvides the <code>zsh</code> language, supports these file types: <code>zsh</code>.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AgGet the parser for ada.1ChDetermines if all elements of the iterator satisfy a \xe2\x80\xa6ChDetermines if any elements of the iterator satisfy a \xe2\x80\xa6AgGet the parser for asm.004AgGet the parser for awk.55AgGet the parser for bib.6CeThe \xe2\x80\x98boo\xe2\x80\x99 theme collection in this module was \xe2\x80\xa67AgGet the parser for bzl.8AgGet the parser for cpp.00AgGet the parser for css.00:AgGet the parser for csv.;;AgGet the parser for cue.<<AgGet the parser for dot.==AgGet the parser for dtd.>>AgGet the parser for dts.??AgGet the parser for eds.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AgGet the parser for ejs.001AgGet the parser for elm.2AgGet the parser for erb.003AgGet the parser for fsd.44AgGet the parser for fsh.55AgGet the parser for gap.6CjReturns a reference to the style corresponding to the key.CkReturns a reference to the current element of the iterator.1ChGet an instance of the respective <code>Language</code>.009AgGet the parser for gpg.::AgGet the parser for hcl.;;AgGet the parser for idl.<<AgGet the parser for ini.==AgGet the parser for kcl.>>AgGet the parser for kdl.??AgGet the parser for kos.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AgGet the parser for leo.1AgGet the parser for lua.00CiCreates an iterator which transforms elements of this \xe2\x80\xa6BoCreate a new, empty list of <code>Sexpr</code>sCdCreate a new, empty list of <code>OwnedSexpr</code>sDeCreate a new <code>Processor</code> given a <code>LanguageSet</code>.EgCreate a new <code>HtmlRenderer</code>. Because <code>HtmlRenderer</code> is a unit \xe2\x80\xa6DjCreate a new <code>TerminalRenderer</code> with an optional background \xe2\x80\xa6EgCreates a <code>HighlightConfiguration</code> for a given <code>Language</code> and \xe2\x80\xa6EjCreate a new <code>Union</code> set by combining <code>left</code> and <code>right</code>.B`Create a new <code>Style</code>.EeCreate a new <code>Theme</code> from a map of theme keys to <code>ThemeValue</code>s.DgCreate a new <code>ResolvedTheme</code> from a map of theme keys to \xe2\x80\xa6AdCreate a new parser.C`Create a new cursor for executing a given query.CkCreate a new query from a string containing one or more \xe2\x80\xa6765437DgCreate a new <code>LanguageSetImpl</code> with no pre-loaded languages.DjCreate a new <code>LanguageLoader</code> which searches for parsers in \xe2\x80\xa611Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AgGet the parser for nim.1AgGet the parser for nix.002AgGet the parser for nqc.3DiConsumes the first <code>n</code> elements of the iterator, returning \xe2\x80\xa6CeThe \xe2\x80\x98one\xe2\x80\x99 theme collection in this module was \xe2\x80\xa65AgGet the parser for pem.66AgGet the parser for pgn.7AgGet the parser for php.008AgGet the parser for pkl.99AgGet the parser for pod.::AgGet the parser for ps1.;;AgGet the parser for psv.<<AgGet the parser for pug.==AgGet the parser for qml.>>AgGet the parser for rbs.?CnThe amount of red light, where 0.0 is no red light and 1.0 \xe2\x80\xa60CmCreates an iterator which returns elemens in the opposite \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AgGet the parser for roc.11AgGet the parser for ron.22AgGet the parser for rst.3AgGet the parser for sql.004AgGet the parser for tal.55AgGet the parser for tcl.66AgGet the parser for tex.77AgGet the parser for tla.88AgGet the parser for tsv.9AgGet the parser for tsx.00:AgGet the parser for usd.;;AgGet the parser for vim.<<AgGet the parser for vrl.==AgGet the parser for vto.>>AgGet the parser for vue.??AgGet the parser for wat.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AgGet the parser for wit.11AgGet the parser for xml.2AgGet the parser for zig.003CiGet the parser for zsh. (not supported on WebAssembly \xe2\x80\xa64Bc(De)serialized as <code>a2ps</code>0Bc(De)serialized as <code>abap</code>0Bc(De)serialized as <code>abel</code>0Bc(De)serialized as <code>abnf</code>0Bc(De)serialized as <code>agda</code>0EhProvides the <code>agda</code> language, supports these file types: <code>agda</code>\xe2\x80\xa6Bc(De)serialized as <code>ahdl</code>0Bc(De)serialized as <code>aidl</code>0Bc(De)serialized as <code>ampl</code>0Bc(De)serialized as <code>apex</code>0EhProvides the <code>apex</code> language, supports these file types: <code>apex</code>\xe2\x80\xa6Bc(De)serialized as <code>arch</code>0Dk(De)serialized as <code>aspx</code>, <strong>not</strong> <code>aspx</code>0CmA sequence of bytes not including whitespace, parens, and \xe2\x80\xa60Dk(De)serialized as <code>ats2</code>, <strong>not</strong> <code>ats2</code>0Bc(De)serialized as <code>bash</code>0EhProvides the <code>bash</code> language, supports these file types: <code>sh</code>, \xe2\x80\xa600Bc(De)serialized as <code>bass</code>0EhProvides the <code>bass</code> language, supports these file types: <code>bass</code>\xe2\x80\xa6Bc(De)serialized as <code>beef</code>0Dk(De)serialized as <code>bmax</code>, <strong>not</strong> <code>bmax</code>0Bc(De)serialized as <code>bsdl</code>0Bc(De)serialized as <code>cmod</code>0CdA character to match literally (eg. <code>a</code>).AcA literal characterDk(De)serialized as <code>chpl</code>, <strong>not</strong> <code>chpl</code>0Bc(De)serialized as <code>clue</code>0Bc(De)serialized as <code>coco</code>0Bc(De)serialized as <code>conf</code>0Dk(De)serialized as <code>cook</code>, <strong>not</strong> <code>cook</code>0EhProvides the <code>cook</code> language, supports these file types: <code>cook</code>\xe2\x80\xa6Bc(De)serialized as <code>corn</code>0EhProvides the <code>corn</code> language, supports these file types: <code>corn</code>\xe2\x80\xa6Bc(De)serialized as <code>cpon</code>0EhProvides the <code>cpon</code> language, supports these file types: <code>cpon</code>\xe2\x80\xa6Bc(De)serialized as <code>csdl</code>0Bc(De)serialized as <code>cson</code>0Bc(De)serialized as <code>cuda</code>0EhProvides the <code>cuda</code> language, supports these file types: <code>cuda</code>\xe2\x80\xa6Bc(De)serialized as <code>cupl</code>0Bc(De)serialized as <code>cweb</code>0Bc(De)serialized as <code>cylc</code>0EhProvides the <code>cylc</code> language, supports these file types: <code>cylc</code>\xe2\x80\xa6Bc(De)serialized as <code>dart</code>0EhProvides the <code>dart</code> language, supports these file types: <code>dart</code>\xe2\x80\xa6Bc(De)serialized as <code>desc</code>0Dn(De)serialized as <code>gitdiff</code>, <strong>not</strong> <code>diff</code>0DgProvides the <code>diff</code> language, supports these file types: \xe2\x80\xa600Bc(De)serialized as <code>diva</code>0Bc(De)serialized as <code>djot</code>0EhProvides the <code>djot</code> language, supports these file types: <code>djot</code>\xe2\x80\xa6Bc(De)serialized as <code>dune</code>0Bc(De)serialized as <code>ebnf</code>0EhProvides the <code>ebnf</code> language, supports these file types: <code>ebnf</code>\xe2\x80\xa6Bc(De)serialized as <code>edge</code>0Bc(De)serialized as <code>edif</code>0Bc(De)serialized as <code>elsa</code>0EhProvides the <code>elsa</code> language, supports these file types: <code>elsa</code>\xe2\x80\xa6Dk(De)serialized as <code>esdl</code>, <strong>not</strong> <code>esdl</code>0Bc(De)serialized as <code>exim</code>0Bc(De)serialized as <code>fidl</code>0EhProvides the <code>fidl</code> language, supports these file types: <code>fidl</code>\xe2\x80\xa6Bc(De)serialized as <code>fish</code>0EhProvides the <code>fish</code> language, supports these file types: <code>fish</code>\xe2\x80\xa600Bc(De)serialized as <code>flix</code>0Bc(De)serialized as <code>foam</code>0EhProvides the <code>foam</code> language, supports these file types: <code>foam</code>\xe2\x80\xa6Bc(De)serialized as <code>form</code>0Di(De)serialized as <code>4d</code>, <strong>not</strong> <code>ft4d</code>0Bc(De)serialized as <code>func</code>0EhProvides the <code>func</code> language, supports these file types: <code>func</code>\xe2\x80\xa6Bc(De)serialized as <code>fvwm</code>0Bc(De)serialized as <code>gaml</code>0Bc(De)serialized as <code>gams</code>0Dk(De)serialized as <code>gdmo</code>, <strong>not</strong> <code>gdmo</code>0Bc(De)serialized as <code>gift</code>0Bc(De)serialized as <code>glsl</code>0EhProvides the <code>glsl</code> language, supports these file types: <code>glsl</code>\xe2\x80\xa6Bc(De)serialized as <code>golo</code>0Bc(De)serialized as <code>gren</code>0EhProvides the <code>gren</code> language, supports these file types: <code>gren</code>\xe2\x80\xa6Bc(De)serialized as <code>groq</code>0EhProvides the <code>groq</code> language, supports these file types: <code>groq</code>\xe2\x80\xa6Bc(De)serialized as <code>grub</code>0Bc(De)serialized as <code>hack</code>0EhProvides the <code>hack</code> language, supports these file types: <code>hack</code>\xe2\x80\xa6Bc(De)serialized as <code>haml</code>0Bc(De)serialized as <code>hare</code>0EhProvides the <code>hare</code> language, supports these file types: <code>hare</code>\xe2\x80\xa6Dk(De)serialized as <code>haxe</code>, <strong>not</strong> <code>haxe</code>0EhProvides the <code>haxe</code> language, supports these file types: <code>haxe</code>\xe2\x80\xa6Dk(De)serialized as <code>heex</code>, <strong>not</strong> <code>heex</code>0EhProvides the <code>heex</code> language, supports these file types: <code>heex</code>\xe2\x80\xa6Bc(De)serialized as <code>helm</code>0EhProvides the <code>helm</code> language, supports these file types: <code>helm</code>\xe2\x80\xa6EhProvides the <code>help</code> language, supports these file types: <code>help</code>\xe2\x80\xa6Dk(De)serialized as <code>hlsl</code>, <strong>not</strong> <code>hlsl</code>0EhProvides the <code>hlsl</code> language, supports these file types: <code>hlsl</code>\xe2\x80\xa6Bc(De)serialized as <code>hoon</code>0EhProvides the <code>hoon</code> language, supports these file types: <code>hoon</code>\xe2\x80\xa6Dk(De)serialized as <code>html</code>, <strong>not</strong> <code>html</code>0EhProvides the <code>html</code> language, supports these file types: <code>html</code>\xe2\x80\xa600Bc(De)serialized as <code>http</code>0EhProvides the <code>http</code> language, supports these file types: <code>http</code>\xe2\x80\xa6Bc(De)serialized as <code>hurl</code>0EhProvides the <code>hurl</code> language, supports these file types: <code>hurl</code>\xe2\x80\xa6Bc(De)serialized as <code>hxml</code>0Bc(De)serialized as <code>hylo</code>0Dk(De)serialized as <code>ical</code>, <strong>not</strong> <code>ical</code>0Bc(De)serialized as <code>icon</code>0Dk(De)serialized as <code>igor</code>, <strong>not</strong> <code>igor</code>0Bc(De)serialized as <code>imba</code>0Bc(De)serialized as <code>inko</code>0EhProvides the <code>inko</code> language, supports these file types: <code>inko</code>\xe2\x80\xa6Bc(De)serialized as <code>ioke</code>0Bc(De)serialized as <code>ipkg</code>0Bc(De)serialized as <code>ispc</code>0EhProvides the <code>ispc</code> language, supports these file types: <code>ispc</code>\xe2\x80\xa6BmThe type of the elements being iterated over.Bc(De)serialized as <code>java</code>0EhProvides the <code>java</code> language, supports these file types: <code>java</code>\xe2\x80\xa600Dk(De)serialized as <code>jess</code>, <strong>not</strong> <code>jess</code>0Dk(De)serialized as <code>json</code>, <strong>not</strong> <code>json</code>0EhProvides the <code>json</code> language, supports these file types: <code>json</code>\xe2\x80\xa600Dk(De)serialized as <code>just</code>, <strong>not</strong> <code>just</code>0EhProvides the <code>just</code> language, supports these file types: <code>just</code>\xe2\x80\xa6Dk(De)serialized as <code>kivy</code>, <strong>not</strong> <code>kivy</code>0Bc(De)serialized as <code>koka</code>0Bc(De)serialized as <code>koto</code>0EhProvides the <code>koto</code> language, supports these file types: <code>koto</code>\xe2\x80\xa6Bc(De)serialized as <code>lace</code>0CnAn enum of every supported language in the current feature \xe2\x80\xa6AoA successfully loaded language.11Bc(De)serialized as <code>lark</code>0Bc(De)serialized as <code>ldif</code>0Dk(De)serialized as <code>lean</code>, <strong>not</strong> <code>lean</code>0Bc(De)serialized as <code>leex</code>0AkA language of the left set.0Dk(De)serialized as <code>less</code>, <strong>not</strong> <code>less</code>0Bc(De)serialized as <code>lftp</code>0Bc(De)serialized as <code>lilo</code>0Dk(De)serialized as <code>lisp</code>, <strong>not</strong> <code>lisp</code>0EhProvides the <code>lisp</code> language, supports these file types: <code>lisp</code>\xe2\x80\xa6EfA list of <code>Sexpr</code>s surrounded by parentheses <code>(</code>, <code>)</code>EkA list of <code>OwnedSexpr</code>s surrounded by parentheses <code>(</code>, <code>)</code>Bc(De)serialized as <code>lite</code>0Bc(De)serialized as <code>llvm</code>0EhProvides the <code>llvm</code> language, supports these file types: <code>llvm</code>\xe2\x80\xa6Bc(De)serialized as <code>lout</code>0Bc(De)serialized as <code>luap</code>0D`Provides the <code>luap</code> language, supports no file types.Bc(De)serialized as <code>luau</code>0EhProvides the <code>luau</code> language, supports these file types: <code>luau</code>\xe2\x80\xa6Bc(De)serialized as <code>lynx</code>0Dk(De)serialized as <code>m68k</code>, <strong>not</strong> <code>m68k</code>0Dk(De)serialized as <code>mail</code>, <strong>not</strong> <code>mail</code>0Dk(De)serialized as <code>make</code>, <strong>not</strong> <code>make</code>0EhProvides the <code>make</code> language, supports these file types: <code>make</code>\xe2\x80\xa600Bc(De)serialized as <code>mako</code>0Bc(De)serialized as <code>mask</code>0Bc(De)serialized as <code>masm</code>0Bc(De)serialized as <code>mint</code>0Bc(De)serialized as <code>mlir</code>0EhProvides the <code>mlir</code> language, supports these file types: <code>mlir</code>\xe2\x80\xa6Bc(De)serialized as <code>mmix</code>0Bc(De)serialized as <code>mojo</code>0Bc(De)serialized as <code>monk</code>0Bc(De)serialized as <code>move</code>0Bc(De)serialized as <code>mql4</code>0Bc(De)serialized as <code>mql5</code>0Bc(De)serialized as <code>msql</code>0Bc(De)serialized as <code>mtml</code>0Bc(De)serialized as <code>mush</code>0Bc(De)serialized as <code>n1ql</code>0Bc(De)serialized as <code>nasl</code>0Dk(De)serialized as <code>nasm</code>, <strong>not</strong> <code>nasm</code>0EhProvides the <code>nasm</code> language, supports these file types: <code>nasm</code>\xe2\x80\xa6Bc(De)serialized as <code>nesc</code>0C`A single node within a syntax <code>Tree</code>.iNo value.Bc(De)serialized as <code>norg</code>0Bc(De)serialized as <code>nsis</code>0Dk(De)serialized as <code>objc</code>, <strong>not</strong> <code>objc</code>0Dl(De)serialized as <code>obj-j</code>, <strong>not</strong> <code>objj</code>0EhProvides the <code>objc</code> language, supports these file types: <code>objc</code>\xe2\x80\xa6Bc(De)serialized as <code>obse</code>0Dk(De)serialized as <code>odin</code>, <strong>not</strong> <code>odin</code>0EhProvides the <code>odin</code> language, supports these file types: <code>odin</code>\xe2\x80\xa6Bc(De)serialized as <code>opal</code>0Bc(De)serialized as <code>opam</code>0Dk(De)serialized as <code>opts</code>, <strong>not</strong> <code>opts</code>0Bc(De)serialized as <code>pact</code>0Bc(De)serialized as <code>papp</code>0Dk(De)serialized as <code>pasm</code>, <strong>not</strong> <code>pasm</code>0Bc(De)serialized as <code>pawn</code>0Bc(De)serialized as <code>pcmk</code>0Bc(De)serialized as <code>pddl</code>0Bc(De)serialized as <code>pep8</code>0Dk(De)serialized as <code>perl</code>, <strong>not</strong> <code>perl</code>0EhProvides the <code>perl</code> language, supports these file types: <code>perl</code>\xe2\x80\xa6Bc(De)serialized as <code>pike</code>0Bc(De)serialized as <code>pine</code>0Bc(De)serialized as <code>poke</code>0Bc(De)serialized as <code>pony</code>0EhProvides the <code>pony</code> language, supports these file types: <code>pony</code>\xe2\x80\xa6Bc(De)serialized as <code>proc</code>0Bc(De)serialized as <code>prql</code>0EhProvides the <code>prql</code> language, supports these file types: <code>prql</code>\xe2\x80\xa6Dk(De)serialized as <code>qb64</code>, <strong>not</strong> <code>qb64</code>0Bc(De)serialized as <code>racc</code>0Dk(De)serialized as <code>raku</code>, <strong>not</strong> <code>raku</code>0Bc(De)serialized as <code>raml</code>0Bc(De)serialized as <code>rasi</code>0EhProvides the <code>rasi</code> language, supports these file types: <code>rasi</code>\xe2\x80\xa6Bc(De)serialized as <code>rdoc</code>0Bc(De)serialized as <code>re2c</code>0EhProvides the <code>re2c</code> language, supports these file types: <code>re2c</code>\xe2\x80\xa6Dk(De)serialized as <code>rego</code>, <strong>not</strong> <code>rego</code>0EhProvides the <code>rego</code> language, supports these file types: <code>rego</code>\xe2\x80\xa6Bc(De)serialized as <code>reva</code>0Dk(De)serialized as <code>rexx</code>, <strong>not</strong> <code>rexx</code>0Bc(De)serialized as <code>ring</code>0Bc(De)serialized as <code>riot</code>0Bc(De)serialized as <code>rrst</code>0Dk(De)serialized as <code>ruby</code>, <strong>not</strong> <code>ruby</code>0EhProvides the <code>ruby</code> language, supports these file types: <code>ruby</code>\xe2\x80\xa600Bc(De)serialized as <code>rush</code>0EhProvides the <code>rush</code> language, supports these file types: <code>rush</code>\xe2\x80\xa600Dk(De)serialized as <code>rust</code>, <strong>not</strong> <code>rust</code>0EhProvides the <code>rust</code> language, supports these file types: <code>rust</code>\xe2\x80\xa600Bc(De)serialized as <code>sage</code>0Bc(De)serialized as <code>sail</code>0Dk(De)serialized as <code>salt</code>, <strong>not</strong> <code>salt</code>0Bc(De)serialized as <code>sass</code>0Bc(De)serialized as <code>scfg</code>0EhProvides the <code>scfg</code> language, supports these file types: <code>scfg</code>\xe2\x80\xa6Bc(De)serialized as <code>scss</code>0EhProvides the <code>scss</code> language, supports these file types: <code>scss</code>\xe2\x80\xa6Bc(De)serialized as <code>sgml</code>0Bc(De)serialized as <code>shen</code>0Bc(De)serialized as <code>sile</code>0Bc(De)serialized as <code>sisu</code>0Bc(De)serialized as <code>skhd</code>0Bc(De)serialized as <code>slim</code>0EhProvides the <code>slim</code> language, supports these file types: <code>slim</code>\xe2\x80\xa6Bc(De)serialized as <code>smcl</code>0Bc(De)serialized as <code>smil</code>0BbSome value of type <code>T</code>.Bc(De)serialized as <code>soql</code>0EhProvides the <code>soql</code> language, supports these file types: <code>soql</code>\xe2\x80\xa6Bc(De)serialized as <code>sosl</code>0EhProvides the <code>sosl</code> language, supports these file types: <code>sosl</code>\xe2\x80\xa6Dk(De)serialized as <code>spec</code>, <strong>not</strong> <code>spec</code>0Bc(De)serialized as <code>spup</code>0Bc(De)serialized as <code>sqlj</code>0Bc(De)serialized as <code>srec</code>0Bc(De)serialized as <code>stan</code>0Bc(De)serialized as <code>ston</code>0Bc(De)serialized as <code>sway</code>0EhProvides the <code>sway</code> language, supports these file types: <code>sway</code>\xe2\x80\xa6Bc(De)serialized as <code>swig</code>0Bc(De)serialized as <code>tact</code>0EhProvides the <code>tact</code> language, supports these file types: <code>tact</code>\xe2\x80\xa6Bc(De)serialized as <code>tads</code>0Bc(De)serialized as <code>tags</code>0EeProvides the <code>tape</code> language, supports these file types: <code>vhs</code>.Bc(De)serialized as <code>tcsh</code>0Dk(De)serialized as <code>teal</code>, <strong>not</strong> <code>teal</code>0EhProvides the <code>teal</code> language, supports these file types: <code>teal</code>\xe2\x80\xa6Bc(De)serialized as <code>tera</code>0EhProvides the <code>tera</code> language, supports these file types: <code>tera</code>\xe2\x80\xa6CcA plain text file. This is the default variant. \xe2\x80\xa60Bc(De)serialized as <code>tidy</code>0Bc(De)serialized as <code>tmux</code>0EhProvides the <code>tmux</code> language, supports these file types: <code>tmux</code>\xe2\x80\xa6Bc(De)serialized as <code>toit</code>0Bc(De)serialized as <code>toml</code>0EhProvides the <code>toml</code> language, supports these file types: <code>toml</code>\xe2\x80\xa6CnA tree that represents the syntactic structure of a source \xe2\x80\xa6Bc(De)serialized as <code>trig</code>0Bc(De)serialized as <code>tsql</code>0Bc(De)serialized as <code>twig</code>0EhProvides the <code>twig</code> language, supports these file types: <code>twig</code>\xe2\x80\xa6Bc(De)serialized as <code>ursa</code>0EhProvides the <code>ursa</code> language, supports these file types: <code>ursa</code>\xe2\x80\xa6Bc(De)serialized as <code>vala</code>0EhProvides the <code>vala</code> language, supports these file types: <code>vala</code>\xe2\x80\xa6Bc(De)serialized as <code>vera</code>0Bc(De)serialized as <code>vhdl</code>0EhProvides the <code>vhdl</code> language, supports these file types: <code>vhdl</code>\xe2\x80\xa6Bc(De)serialized as <code>volt</code>0Bc(De)serialized as <code>vrml</code>0Dk(De)serialized as <code>wast</code>, <strong>not</strong> <code>wast</code>0Dk(De)serialized as <code>wget</code>, <strong>not</strong> <code>wget</code>0Bc(De)serialized as <code>wgsl</code>0EhProvides the <code>wgsl</code> language, supports these file types: <code>wgsl</code>\xe2\x80\xa6Bc(De)serialized as <code>wing</code>0EhProvides the <code>wing</code> language, supports these file types: <code>wing</code>\xe2\x80\xa6Bc(De)serialized as <code>wisp</code>0Bc(De)serialized as <code>wsml</code>0Bc(De)serialized as <code>wxml</code>0EhProvides the <code>wxml</code> language, supports these file types: <code>wxml</code>\xe2\x80\xa6Bc(De)serialized as <code>xojo</code>0Bc(De)serialized as <code>xpm2</code>0Dk(De)serialized as <code>xslt</code>, <strong>not</strong> <code>xslt</code>0Dk(De)serialized as <code>xten</code>, <strong>not</strong> <code>xten</code>0Bc(De)serialized as <code>yacc</code>0Dk(De)serialized as <code>yaml</code>, <strong>not</strong> <code>yaml</code>0EhProvides the <code>yaml</code> language, supports these file types: <code>yaml</code>\xe2\x80\xa600Bc(De)serialized as <code>yang</code>0EhProvides the <code>yang</code> language, supports these file types: <code>yang</code>\xe2\x80\xa6Bc(De)serialized as <code>yara</code>0Bc(De)serialized as <code>yasm</code>0Bc(De)serialized as <code>yuck</code>0EhProvides the <code>yuck</code> language, supports these file types: <code>yuck</code>\xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AhGet the parser for agda.11AhGet the parser for apex.2AhGet the parser for bash.003AhGet the parser for bass.4ClThe amount of blue light, where 0.0 is no blue light and \xe2\x80\xa60BhWhether this <code>Style</code> is bold.D`Whether the text should be bold. (default is <code>false</code>)107AhGet the parser for cook.88AhGet the parser for corn.99AhGet the parser for cpon.::AhGet the parser for cuda.;;AhGet the parser for cylc.<<AhGet the parser for dart.=AhGet the parser for diff.00>AhGet the parser for djot.??AhGet the parser for ebnf.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6ClEdit the syntax tree to keep it in sync with source code \xe2\x80\xa6CkEdit this node to keep it in-sync with source code that \xe2\x80\xa62AhGet the parser for elsa.33AhGet the parser for fidl.4ClReturns the first element of the iterator that satisfies \xe2\x80\xa6AhGet the parser for fish.006AhGet the parser for foam.7CkReduces the iterator\xe2\x80\x99s elements to a single, final value.AoReturns the argument unchanged. the parser for func.:CjCreates an iterator which is \xe2\x80\x9cwell behaved\xe2\x80\x9d at the \xe2\x80\xa6;AhGet the parser for glsl.<<AhGet the parser for gren.==AhGet the parser for groq.>>AhGet the parser for hack.??AhGet the parser for hare.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AhGet the parser for haxe.1CnIs called at the start of rendering, before anything else. \xe2\x80\xa62AhGet the parser for heex.33AhGet the parser for helm.44AhGet the parser for help.55AhGet the parser for hlsl.66AhGet the parser for hoon.7AhGet the parser for html.008AhGet the parser for http.99AhGet the parser for hurl.::AhGet the parser for inko.;BaCalls <code>U::from(self)</code>.<AhGet the parser for ispc.=AhGet the parser for java.00AhGet the parser for json.00?AhGet the parser for just.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6BcGet this node\xe2\x80\x99s type as a string.1AhGet the parser for koto.2B`Get a reference to the left set.0BmA link to the theme entry with the given key.04AhGet the parser for lisp.55CjGet the parser for llvm. (not supported on WebAssembly \xe2\x80\xa666AhGet the parser for luap.77AhGet the parser for luau.8AhGet the parser for make.009AhGet the parser for mlir.:AoGet the name for this language.DjGet the name of this language. This returns <code>None</code> in older \xe2\x80\xa610<AhGet the parser for nasm.=CfThe \xe2\x80\x98neon\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6CaAdvances the iterator and returns the next value.CbGet the tree cursor\xe2\x80\x99s current <code>Node</code>.CfThe \xe2\x80\x98nord\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AhGet the parser for objc.11AhGet the parser for odin.2CfThe \xe2\x80\x98omni\xe2\x80\x99 theme collection in this module was \xe2\x80\xa63AhGet the parser for perl.44AhGet the parser for pony.55AhGet the parser for prql.66AhGet the parser for rasi.77AhGet the parser for re2c.88AhGet the parser for rego.9AhGet the parser for ruby.00AhGet the parser for rush.00AhGet the parser for rust.00<AhGet the parser for scfg.==AhGet the parser for scss.>DbCreates an iterator which skips the first <code>n</code> elements.?AhGet the parser for slim.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AhGet the parser for soql.11AhGet the parser for sosl.22AhGet the parser for sway.33AhGet the parser for tact.4CnIs called at the end of rendering, after the final call to \xe2\x80\xa6DcCreates an iterator which only returns the first <code>n</code> \xe2\x80\xa66AhGet the parser for tape.77AhGet the parser for teal.88AhGet the parser for tera.99CjGet the parser for tmux. (not supported on WebAssembly \xe2\x80\xa6::AhGet the parser for toml.;;AhGet the parser for twig.<<AhGet the parser for ursa.==AhGet the parser for vala.>>AhGet the parser for vhdl.?DhCreate a new <code>TreeCursor</code> starting from the root of the tree.CmCreate a new <code>TreeCursor</code> starting from this node.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AhGet the parser for wgsl.11AhGet the parser for wing.22AhGet the parser for wxml.3AhGet the parser for yaml.004AhGet the parser for yang.55AhGet the parser for yuck.6Bd(De)serialized as <code>acedb</code>0Dm(De)serialized as <code>afdko</code>, <strong>not</strong> <code>afdko</code>0Bd(De)serialized as <code>aiken</code>0Bd(De)serialized as <code>algol</code>0Bd(De)serialized as <code>alloy</code>0Bd(De)serialized as <code>astro</code>0DhProvides the <code>astro</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>atlas</code>, <strong>not</strong> <code>atlas</code>0Bd(De)serialized as <code>basic</code>0Bd(De)serialized as <code>bicep</code>0DhProvides the <code>bicep</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>bison</code>0Bd(De)serialized as <code>blade</code>0DhProvides the <code>blade</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>blank</code>0Dm(De)serialized as <code>byond</code>, <strong>not</strong> <code>byond</code>0Bd(De)serialized as <code>cmake</code>0Bd(De)serialized as <code>cterm</code>0Dm(De)serialized as <code>cabal</code>, <strong>not</strong> <code>cabal</code>0Dm(De)serialized as <code>caddy</code>, <strong>not</strong> <code>caddy</code>0DhProvides the <code>caddy</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>cairo</code>, <strong>not</strong> <code>cairo</code>0DhProvides the <code>cairo</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>capnp</code>, <strong>not</strong> <code>capnp</code>0DhProvides the <code>capnp</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>cedar</code>0Bd(De)serialized as <code>chill</code>0Bd(De)serialized as <code>chuck</code>0Bd(De)serialized as <code>cirru</code>0CiA character class (eg. <code>%w</code>, <code>%L</code>).0AaA character classCnA character class, matching any character contained in the \xe2\x80\xa6Bd(De)serialized as <code>clean</code>0Bd(De)serialized as <code>click</code>0DhProvides the <code>cmake</code> language, supports these file types: \xe2\x80\xa600Bd(De)serialized as <code>cobol</code>0ClA non-transparent color with red, green, and blue values \xe2\x80\xa60Dm(De)serialized as <code>conll</code>, <strong>not</strong> <code>conll</code>0Bd(De)serialized as <code>curry</code>0Bd(De)serialized as <code>cvsrc</code>0Bd(De)serialized as <code>cynpp</code>0Bd(De)serialized as <code>dafny</code>0Bd(De)serialized as <code>dhall</code>0DhProvides the <code>dhall</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>dylan</code>0Dm(De)serialized as <code>eruby</code>, <strong>not</strong> <code>eruby</code>0Bd(De)serialized as <code>eagle</code>0Dm(De)serialized as <code>elisp</code>, <strong>not</strong> <code>elisp</code>0DhProvides the <code>elisp</code> language, supports these file types: \xe2\x80\xa6AnThe crate\xe2\x80\x99s main error type.AeThe crate error type.AdThe main error type.0CeRepresents the reason why syntax highlighting failed.DhProvides the <code>eruby</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>esqlc</code>, <strong>not</strong> <code>esqlc</code>0Bd(De)serialized as <code>eterm</code>0Bd(De)serialized as <code>fancy</code>0Dm(De)serialized as <code>faust</code>, <strong>not</strong> <code>faust</code>0DhProvides the <code>faust</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>forth</code>0DhProvides the <code>forth</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>frege</code>0Bd(De)serialized as <code>fstab</code>0Bd(De)serialized as <code>fstar</code>0Dn(De)serialized as <code>fvwm-1</code>, <strong>not</strong> <code>fvwm1</code>0Dm(De)serialized as <code>fvwm2</code>, <strong>not</strong> <code>fvwm2</code>0Bd(De)serialized as <code>gleam</code>0DhProvides the <code>gleam</code> language, supports these file types: \xe2\x80\xa600Bd(De)serialized as <code>glyph</code>0Bd(De)serialized as <code>gnash</code>0Dm(De)serialized as <code>gomod</code>, <strong>not</strong> <code>gomod</code>0Dm(De)serialized as <code>gosum</code>, <strong>not</strong> <code>gosum</code>0Bd(De)serialized as <code>goctl</code>0DhProvides the <code>goctl</code> language, supports these file types: \xe2\x80\xa6DhProvides the <code>gomod</code> language, supports these file types: \xe2\x80\xa6DhProvides the <code>gosum</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>grace</code>0Dm(De)serialized as <code>grads</code>, <strong>not</strong> <code>grads</code>0Bd(De)serialized as <code>gretl</code>0Bd(De)serialized as <code>groff</code>0EcA list of <code>Sexpr</code>s surrounded by brackets <code>[</code>, <code>]</code>EhA list of <code>OwnedSexpr</code>s surrounded by brackets <code>[</code>, <code>]</code>Bd(De)serialized as <code>group</code>0Bd(De)serialized as <code>gtkrc</code>0Bd(De)serialized as <code>hjson</code>0Bd(De)serialized as <code>haste</code>0DhProvides the <code>hjson</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>hocon</code>0DhProvides the <code>hocon</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>holyc</code>0Dm(De)serialized as <code>hosts</code>, <strong>not</strong> <code>hosts</code>0Bd(De)serialized as <code>hyphy</code>0Dn(De)serialized as <code>idris2</code>, <strong>not</strong> <code>idris</code>0DhProvides the <code>janet</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>jflex</code>0Dm(De)serialized as <code>jinja</code>, <strong>not</strong> <code>jinja</code>0DhProvides the <code>jinja</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>jison</code>0Bd(De)serialized as <code>jolie</code>0Bd(De)serialized as <code>jsdoc</code>0DaProvides the <code>jsdoc</code> language, supports no file types.00Bd(De)serialized as <code>json5</code>0DhProvides the <code>json5</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>jsonc</code>, <strong>not</strong> <code>jsonc</code>0Bd(De)serialized as <code>jsonl</code>0DhProvides the <code>jsonc</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>julia</code>0DhProvides the <code>julia</code> language, supports these file types: \xe2\x80\xa600Bd(De)serialized as <code>karel</code>0Bd(De)serialized as <code>kerml</code>0Bd(De)serialized as <code>kitty</code>0DhProvides the <code>kitty</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>kusto</code>0DhProvides the <code>kusto</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>larch</code>0EhProvides the <code>latex</code> language, supports these file types: <code>tex</code>\xe2\x80\xa6Bd(De)serialized as <code>latte</code>0Bd(De)serialized as <code>libao</code>0Bd(De)serialized as <code>limbo</code>0Bd(De)serialized as <code>lotos</code>0Bd(De)serialized as <code>magik</code>0DhProvides the <code>magik</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>maple</code>, <strong>not</strong> <code>maple</code>0Bd(De)serialized as <code>mason</code>0Bd(De)serialized as <code>meson</code>0DhProvides the <code>meson</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>metal</code>0Bd(De)serialized as <code>minid</code>0Bd(De)serialized as <code>mirah</code>0Bd(De)serialized as <code>msidl</code>0Bd(De)serialized as <code>msmtp</code>0Dm(De)serialized as <code>mumps</code>, <strong>not</strong> <code>mumps</code>0Bd(De)serialized as <code>mupad</code>0Bd(De)serialized as <code>mysql</code>0Bd(De)serialized as <code>named</code>0Bd(De)serialized as <code>nasal</code>0Bd(De)serialized as <code>netrc</code>0Dm(De)serialized as <code>nginx</code>, <strong>not</strong> <code>nginx</code>0DhProvides the <code>nginx</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>ninja</code>0DhProvides the <code>ninja</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>nmodl</code>0Dm(De)serialized as <code>nroff</code>, <strong>not</strong> <code>nroff</code>0Bd(De)serialized as <code>numpy</code>0Bd(De)serialized as <code>ocaml</code>0Dm(De)serialized as <code>oasv2</code>, <strong>not</strong> <code>oasv2</code>0Dm(De)serialized as <code>oasv3</code>, <strong>not</strong> <code>oasv3</code>0DhProvides the <code>ocaml</code> language, supports these file types: \xe2\x80\xa600Bd(De)serialized as <code>occam</code>0Bd(De)serialized as <code>ondir</code>0Bd(De)serialized as <code>pinfo</code>0Dm(De)serialized as <code>pbtxt</code>, <strong>not</strong> <code>pbtxt</code>0DhProvides the <code>pbtxt</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>pccts</code>, <strong>not</strong> <code>pccts</code>0Dn(De)serialized as <code>peg-js</code>, <strong>not</strong> <code>pegjs</code>0Bd(De)serialized as <code>pilrc</code>0Bd(De)serialized as <code>plsql</code>0CnA position in a multi-line text document, in terms of rows \xe2\x80\xa6Bd(De)serialized as <code>polar</code>0Bd(De)serialized as <code>ppwiz</code>0Bd(De)serialized as <code>praat</code>0Dm(De)serialized as <code>proto</code>, <strong>not</strong> <code>proto</code>0DhProvides the <code>proto</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>pycon</code>, <strong>not</strong> <code>pycon</code>0Bd(De)serialized as <code>pyret</code>0Dm(De)serialized as <code>pyrex</code>, <strong>not</strong> <code>pyrex</code>0Bd(De)serialized as <code>qmake</code>0Dk(De)serialized as <code>qml</code>, <strong>not</strong> <code>qmljs</code>0Bd(De)serialized as <code>quake</code>0CdA set of patterns that match nodes in a syntax tree.DhProvides the <code>query</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>rhelp</code>, <strong>not</strong> <code>rhelp</code>0Bd(De)serialized as <code>ralph</code>0DhProvides the <code>ralph</code> language, supports these file types: \xe2\x80\xa6DjA range of characters (eg. <code>a-z</code>). Matches if any character \xe2\x80\xa6ClA range of positions in a multi-line text document, both \xe2\x80\xa6Bd(De)serialized as <code>rapid</code>0Bd(De)serialized as <code>razor</code>0DhProvides the <code>razor</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>rebol</code>0Bd(De)serialized as <code>redif</code>0Dm(De)serialized as <code>regex</code>, <strong>not</strong> <code>regex</code>0DaProvides the <code>regex</code> language, supports no file types.00Dm(De)serialized as <code>renpy</code>, <strong>not</strong> <code>renpy</code>0AlA language of the right set.0Dm(De)serialized as <code>robot</code>, <strong>not</strong> <code>robot</code>0DhProvides the <code>robot</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>rouge</code>0BbRound parentheses: <code>()</code>Dm(De)serialized as <code>rpgle</code>, <strong>not</strong> <code>rpgle</code>0Bd(De)serialized as <code>samba</code>0Bd(De)serialized as <code>scala</code>0DhProvides the <code>scala</code> language, supports these file types: \xe2\x80\xa600Bd(De)serialized as <code>scaml</code>0Bd(De)serialized as <code>scdoc</code>0EeA single node of the tree. The <code>Atom</code> and <code>String</code> variants \xe2\x80\xa6DhProvides the <code>sface</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>sflog</code>0DhProvides the <code>sflog</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>sieve</code>0Bd(De)serialized as <code>sinda</code>0Bd(De)serialized as <code>skill</code>0Ec(De)serialized as <code>shaderslang</code>, <strong>not</strong> <code>slang</code>0Bd(De)serialized as <code>slice</code>0Bd(De)serialized as <code>slint</code>0DhProvides the <code>slint</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>smali</code>0DhProvides the <code>smali</code> language, supports these file types: \xe2\x80\xa6Dm(De)serialized as <code>smith</code>, <strong>not</strong> <code>smith</code>0Bd(De)serialized as <code>soong</code>0Dm(De)serialized as <code>spice</code>, <strong>not</strong> <code>spice</code>0Bd(De)serialized as <code>spyce</code>0Bd(De)serialized as <code>sqlpl</code>0Bd(De)serialized as <code>squid</code>0BoMatch the start of the string (<code>^</code>).n<code>^</code>Bd(De)serialized as <code>stata</code>0BfDefines how to style a region of text.0Dm(De)serialized as <code>surql</code>, <strong>not</strong> <code>surql</code>0Bd(De)serialized as <code>swift</code>0DhProvides the <code>swift</code> language, supports these file types: \xe2\x80\xa60Bd(De)serialized as <code>sysml</code>0Bd(De)serialized as <code>talon</code>0Bd(De)serialized as <code>templ</code>0DhProvides the <code>templ</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>terra</code>0Bd(De)serialized as <code>texmf</code>0CjA raw theme which may contain links to other items inside.0Bd(De)serialized as <code>tiasm</code>0Bd(De)serialized as <code>tiger</code>0DhProvides the <code>tiger</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>tilde</code>0CiA token as used by the internal lexer. Exposed to the \xe2\x80\xa6Dm(De)serialized as <code>torrc</code>, <strong>not</strong> <code>torrc</code>0Bd(De)serialized as <code>tsalt</code>0Bd(De)serialized as <code>tsscl</code>0Bd(De)serialized as <code>tssgm</code>0Bd(De)serialized as <code>tssop</code>0Bd(De)serialized as <code>tutor</code>0Dm(De)serialized as <code>typst</code>, <strong>not</strong> <code>typst</code>0DhProvides the <code>typst</code> language, supports these file types: \xe2\x80\xa6DbA combination of two arbitrary <code>LanguageSet</code>s into one.0Dn(De)serialized as <code>ur/web</code>, <strong>not</strong> <code>urweb</code>0Bd(De)serialized as <code>vdmpp</code>0Bd(De)serialized as <code>vdmrt</code>0Bd(De)serialized as <code>vdmsl</code>0Dk(De)serialized as <code>vto</code>, <strong>not</strong> <code>vento</code>0Bd(De)serialized as <code>vmasm</code>0Bd(De)serialized as <code>voscm</code>0Bd(De)serialized as <code>vroom</code>0Bd(De)serialized as <code>vyper</code>0Bd(De)serialized as <code>wget2</code>0Bd(De)serialized as <code>xmath</code>0Bd(De)serialized as <code>xhtml</code>0Bd(De)serialized as <code>xmake</code>0Bd(De)serialized as <code>xonsh</code>0Bd(De)serialized as <code>xproc</code>0Bd(De)serialized as <code>xtend</code>0Bd(De)serialized as <code>ziggy</code>0DhProvides the <code>ziggy</code> language, supports these file types: \xe2\x80\xa6Bd(De)serialized as <code>zimbu</code>0Bd(De)serialized as <code>zimpl</code>0CgThe \xe2\x80\x98abscs\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AiGet the parser for astro.11AiGet the parser for bicep.22AiGet the parser for blade.33AiGet the parser for caddy.44AiGet the parser for cairo.55AiGet the parser for capnp.6CjConsumes two iterators and returns a new iterator that \xe2\x80\xa6CiGet the node\xe2\x80\x99s child at the given index, where zero \xe2\x80\xa6AiGet the parser for cmake.00CnGet this <code>Style</code>\xe2\x80\x99s foreground <code>Color</code>.CnThe foreground color to use for this style, specified as a \xe2\x80\xa610CkConsumes the iterator, counting the number of remaining \xe2\x80\xa6ClGet the depth of the cursor\xe2\x80\x99s current node relative to \xe2\x80\xa6=AiGet the parser for dhall.>>AiGet the parser for elisp.??AiGet the parser for eruby.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AiGet the parser for faust.11AiGet the parser for forth.2AiGet the parser for gleam.003AiGet the parser for goctl.44AiGet the parser for gomod.55AiGet the parser for gosum.6CnThe amount of green light, where 0.0 is no green light and \xe2\x80\xa607AiGet the parser for hjson.88AiGet the parser for hocon.99AiGet the parser for janet.::AiGet the parser for jinja.;AiGet the parser for jsdoc.00<AiGet the parser for json5.==AiGet the parser for jsonc.>AiGet the parser for julia.00?CkGet the parser for kitty. (not supported on WebAssembly \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AiGet the parser for kusto.11AiGet the parser for latex.22AiGet the parser for magik.33AiGet the parser for meson.4CmGet a slice containing all of the highlight names used in \xe2\x80\xa6005AiGet the parser for nginx.66AiGet the parser for ninja.7AiGet the parser for ocaml.00BnParse the given input string as a Lua pattern.AkParse a slice of UTF8 text.:AiGet the parser for pbtxt.;;AiGet the parser for proto.<<AiGet the parser for query.==AiGet the parser for ralph.>CkGet the range of source code that this node represents, \xe2\x80\xa6?AiGet the parser for razor.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AiGet the parser for regex.00ChInstruct the parser to start the next parse from the \xe2\x80\xa6AmReset the lookahead iterator.CkRe-initialize this tree cursor to start at the original \xe2\x80\xa6BaGet a reference to the right set.05AiGet the parser for robot.6AiGet the parser for scala.007AiGet the parser for sface.88AiGet the parser for sflog.99AiGet the parser for slint.::AiGet the parser for smali.;F`Defines the <code>Style</code> and <code>Color</code> types used by <code>ResolvedTheme</code>s.0AiGet the parser for swift.0==AiGet the parser for templ.>BlDefines items related to theming the output.CkConvenience macro for constructing new <code>Theme</code>s.01Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AiGet the parser for tiger.1CgThe \xe2\x80\x98tokyo\xe2\x80\x99 theme collection in this module was \xe2\x80\xa62AiGet the parser for typst.33AiGet the parser for ziggy.4Be(De)serialized as <code>abaqus</code>0Do(De)serialized as <code>altium</code>, <strong>not</strong> <code>altium</code>0Do(De)serialized as <code>antlr4</code>, <strong>not</strong> <code>antlr4</code>0Do(De)serialized as <code>apache</code>, <strong>not</strong> <code>apache</code>0DiProvides the <code>asm68k</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>aspvbs</code>0Be(De)serialized as <code>augeas</code>0Do(De)serialized as <code>autoit</code>, <strong>not</strong> <code>autoit</code>0DiProvides the <code>bibtex</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>boogie</code>0Do(De)serialized as <code>csharp</code>, <strong>not</strong> <code>csharp</code>0DjProvides the <code>c_sharp</code> language, supports these file types: \xe2\x80\xa600Be(De)serialized as <code>carbon</code>0Do(De)serialized as <code>cdrtoc</code>, <strong>not</strong> <code>cdrtoc</code>0Be(De)serialized as <code>ceylon</code>0Be(De)serialized as <code>cgdbrc</code>0Be(De)serialized as <code>change</code>0Be(De)serialized as <code>circom</code>0DiProvides the <code>circom</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>cmusrc</code>0Do(De)serialized as <code>config</code>, <strong>not</strong> <code>config</code>0Be(De)serialized as <code>cqlang</code>0Be(De)serialized as <code>creole</code>0DiProvides the <code>csharp</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>curlrc</code>, <strong>not</strong> <code>curlrc</code>0CnA custom error which may be returned by external crates if \xe2\x80\xa60Be(De)serialized as <code>cypher</code>0Do(De)serialized as <code>d2lang</code>, <strong>not</strong> <code>d2lang</code>0Do(De)serialized as <code>dtrace</code>, <strong>not</strong> <code>dtrace</code>0DfMatches any digit; equivalent to <code>[0-9]</code> (<code>%d</code>).Do(De)serialized as <code>dosini</code>, <strong>not</strong> <code>dosini</code>0Be(De)serialized as <code>dotenv</code>0Do(De)serialized as <code>dpatch</code>, <strong>not</strong> <code>dpatch</code>0Be(De)serialized as <code>elinks</code>0Be(De)serialized as <code>eiffel</code>0Do(De)serialized as <code>eighth</code>, <strong>not</strong> <code>eighth</code>0Be(De)serialized as <code>elixir</code>0DiProvides the <code>elixir</code> language, supports these file types: \xe2\x80\xa600Be(De)serialized as <code>elvish</code>0DiProvides the <code>elvish</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>erlang</code>0DiProvides the <code>erlang</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>expect</code>0Do(De)serialized as <code>fsharp</code>, <strong>not</strong> <code>fsharp</code>0Be(De)serialized as <code>factor</code>0Be(De)serialized as <code>falcon</code>0Be(De)serialized as <code>fennel</code>0DiProvides the <code>fennel</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>firrtl</code>0DiProvides the <code>firrtl</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>fluent</code>, <strong>not</strong> <code>fluent</code>0DiProvides the <code>fsharp</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>fusion</code>0DiProvides the <code>fusion</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>gaptst</code>0DiProvides the <code>gaptst</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>gedcom</code>0Do(De)serialized as <code>gowork</code>, <strong>not</strong> <code>gowork</code>0Be(De)serialized as <code>gotmpl</code>0DiProvides the <code>gotmpl</code> language, supports these file types: \xe2\x80\xa6DiProvides the <code>gowork</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>groovy</code>, <strong>not</strong> <code>groovy</code>0DiProvides the <code>groovy</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>hiveql</code>0Be(De)serialized as <code>httest</code>0Be(De)serialized as <code>htmlm4</code>0Be(De)serialized as <code>ibasic</code>0Be(De)serialized as <code>idlang</code>0DiProvides the <code>idris2</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>indent</code>0Be(De)serialized as <code>inform</code>0Be(De)serialized as <code>initng</code>0Be(De)serialized as <code>jgraph</code>0Be(De)serialized as <code>jasmin</code>0Be(De)serialized as <code>javacc</code>0Be(De)serialized as <code>jovial</code>0Be(De)serialized as <code>jsonld</code>0Be(De)serialized as <code>kotlin</code>0DiProvides the <code>kotlin</code> language, supports these file types: \xe2\x80\xa60n<code>[</code>n<code>(</code>Be(De)serialized as <code>ledger</code>0DiProvides the <code>ledger</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>limits</code>0Be(De)serialized as <code>liquid</code>0DiProvides the <code>liquid</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>lookml</code>0Be(De)serialized as <code>luadoc</code>0DiProvides the <code>luadoc</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>lyrics</code>0Be(De)serialized as <code>m17ndb</code>0Be(De)serialized as <code>master</code>0Be(De)serialized as <code>matlab</code>0DiProvides the <code>matlab</code> language, supports these file types: \xe2\x80\xa6E`(De)serialized as <code>max/msp</code>, <strong>not</strong> <code>maxmsp</code>0Be(De)serialized as <code>maxima</code>0Be(De)serialized as <code>mbsync</code>0Be(De)serialized as <code>mdsvex</code>0Be(De)serialized as <code>menhir</code>0DiProvides the <code>menhir</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>monkey</code>0Be(De)serialized as <code>murphi</code>0Be(De)serialized as <code>muttrc</code>0DiProvides the <code>muttrc</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>myghty</code>0Be(De)serialized as <code>nanorc</code>0Do(De)serialized as <code>nickel</code>, <strong>not</strong> <code>nickel</code>0DiProvides the <code>nickel</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>numbat</code>0Be(De)serialized as <code>oberon</code>0Be(De)serialized as <code>objcpp</code>0Be(De)serialized as <code>octave</code>0Do(De)serialized as <code>openrc</code>, <strong>not</strong> <code>openrc</code>0Be(De)serialized as <code>pamenv</code>0Do(De)serialized as <code>pandoc</code>, <strong>not</strong> <code>pandoc</code>0Be(De)serialized as <code>parrot</code>0DfA stateful object that this is used to produce a <code>Tree</code> \xe2\x80\xa6Do(De)serialized as <code>pascal</code>, <strong>not</strong> <code>pascal</code>0DiProvides the <code>pascal</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>passwd</code>0DiProvides the <code>passwd</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>pcbnew</code>, <strong>not</strong> <code>pcbnew</code>0Be(De)serialized as <code>pfmain</code>0Be(De)serialized as <code>phpdoc</code>0DiProvides the <code>phpdoc</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>pikchr</code>, <strong>not</strong> <code>pikchr</code>0Be(De)serialized as <code>pioasm</code>0DiProvides the <code>pioasm</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>povini</code>0Be(De)serialized as <code>printf</code>0DbProvides the <code>printf</code> language, supports no file types.00Be(De)serialized as <code>prisma</code>0DiProvides the <code>prisma</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>prolog</code>0DiProvides the <code>prolog</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>promql</code>0DiProvides the <code>promql</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>ps1xml</code>0Be(De)serialized as <code>puppet</code>0DiProvides the <code>puppet</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>python</code>, <strong>not</strong> <code>python</code>0DiProvides the <code>python</code> language, supports these file types: \xe2\x80\xa600Be(De)serialized as <code>qmldir</code>0DiProvides the <code>qmldir</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>qsharp</code>0Do(De)serialized as <code>quarto</code>, <strong>not</strong> <code>quarto</code>0n<code>]</code>n<code>)</code>Be(De)serialized as <code>racket</code>0DiProvides the <code>racket</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>reason</code>0Be(De)serialized as <code>remind</code>0Be(De)serialized as <code>resolv</code>0DaThe default result type. The error variant is <code>Error</code>.CmThe default result type. The error variant is a vector of \xe2\x80\xa6AeThe main result type.0Do(De)serialized as <code>rnoweb</code>, <strong>not</strong> <code>rnoweb</code>0DiProvides the <code>rnoweb</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>robots</code>, <strong>not</strong> <code>robots</code>0DiProvides the <code>robots</code> language, supports these file types: \xe2\x80\xa6Do(De)serialized as <code>rpcgen</code>, <strong>not</strong> <code>rpcgen</code>0Be(De)serialized as <code>runoff</code>0Be(De)serialized as <code>sather</code>0Be(De)serialized as <code>scenic</code>0Do(De)serialized as <code>scheme</code>, <strong>not</strong> <code>scheme</code>0DiProvides the <code>scheme</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>scilab</code>0Be(De)serialized as <code>screen</code>0EiA thin wrapper around <code>Vec&lt;Sexpr&gt;</code> with its own <code>Display</code> \xe2\x80\xa6DkMay either be a hexadecimal color literal, or a <code>$</code> followed \xe2\x80\xa60Be(De)serialized as <code>simula</code>0Be(De)serialized as <code>slpreg</code>0Be(De)serialized as <code>slpspi</code>0Be(De)serialized as <code>slrnrc</code>0Be(De)serialized as <code>slrnsc</code>0Be(De)serialized as <code>smarty</code>0Be(De)serialized as <code>smithy</code>0DiProvides the <code>smithy</code> language, supports these file types: \xe2\x80\xa6CcMatches any whitespace character; equivalent to \xe2\x80\xa6Be(De)serialized as <code>sparql</code>0DiProvides the <code>sparql</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>sproto</code>0DiProvides the <code>sproto</code> language, supports these file types: \xe2\x80\xa6B`Square brackets: <code>[]</code>Be(De)serialized as <code>strace</code>0DiProvides the <code>strace</code> language, supports these file types: \xe2\x80\xa6CdA sequence of characters to match literally (eg. \xe2\x80\xa6ClA sequence of bytes surrounded by quotes <code>&quot;</code>0Be(De)serialized as <code>styled</code>0DiProvides the <code>styled</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>stylus</code>0Be(De)serialized as <code>svelte</code>0DiProvides the <code>svelte</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>sysctl</code>0DhA list of all theme names as they are accepted by <code>from_str</code>.Be(De)serialized as <code>thrift</code>0DiProvides the <code>thrift</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>trasys</code>0Be(De)serialized as <code>turing</code>0Be(De)serialized as <code>turtle</code>0DiProvides the <code>turtle</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>unison</code>0DiProvides the <code>unison</code> language, supports these file types: \xe2\x80\xa6Be(De)serialized as <code>virata</code>0Be(De)serialized as <code>webidl</code>0Be(De)serialized as <code>whiley</code>0Be(De)serialized as <code>wollok</code>0Be(De)serialized as <code>wvdial</code>0Be(De)serialized as <code>xquery</code>0Be(De)serialized as <code>xinetd</code>0Be(De)serialized as <code>xpages</code>0Be(De)serialized as <code>zephir</code>0Be(De)serialized as <code>zmodel</code>0Do(De)serialized as <code>zserio</code>, <strong>not</strong> <code>zserio</code>0Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AjGet the parser for asm68k.1ChThe \xe2\x80\x98aurora\xe2\x80\x99 theme collection in this module was \xe2\x80\xa62AjGet the parser for bibtex.3BnBorrows an iterator, rather than consuming it.4AjGet the parser for circom.5CmProduces a normal, non-streaming, iterator by cloning the \xe2\x80\xa6CmProduces a normal, non-streaming, iterator by copying the \xe2\x80\xa67AjGet the parser for csharp.8CnA callback that decodes the next code point from the input \xe2\x80\xa6AjGet the parser for elixir.00:AjGet the parser for elvish.;;AjGet the parser for erlang.<ClIs called for every region of the input, both styled and \xe2\x80\xa6ChThe \xe2\x80\x98falcon\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6>AjGet the parser for fennel.?ClCreates an iterator which uses a closure to determine if \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AjGet the parser for firrtl.11AjGet the parser for fsharp.22AjGet the parser for fusion.33AjGet the parser for gaptst.4ChThe \xe2\x80\x98github\xe2\x80\x99 theme collection in this module was \xe2\x80\xa65AjGet the parser for gotmpl.66AjGet the parser for gowork.77AjGet the parser for groovy.88AjGet the parser for idris2.9BjWhether this <code>Style</code> is italic.DbWhether the text should be italic. (default is <code>false</code>)10;AjGet the parser for kotlin.0<AjGet the parser for ledger.==AjGet the parser for liquid.>BbGet the parser\xe2\x80\x99s current logger.?AjGet the parser for luadoc.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AjGet the parser for matlab.11AjGet the parser for menhir.22AjGet the parser for muttrc.33AjGet the parser for nickel.4BnGet this node\xe2\x80\x99s immediate parent. Prefer \xe2\x80\xa65AjGet the parser for pascal.66AjGet the parser for passwd.77AjGet the parser for phpdoc.88AjGet the parser for pioasm.9AjGet the parser for printf.00:AjGet the parser for prisma.;;AjGet the parser for prolog.<<AjGet the parser for promql.==AjGet the parser for puppet.>AjGet the parser for python.00?AjGet the parser for qmldir.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AjGet the parser for racket.1DmRender the given <code>Highlights</code> with a <code>Renderer</code> and \xe2\x80\xa6ChRender code that was previously processed by calling \xe2\x80\xa63AjGet the parser for rnoweb.44AjGet the parser for robots.5frsexpr6AjGet the parser for scheme.77AjGet the parser for smithy.88AjGet the parser for sparql.99AjGet the parser for sproto.::AjGet the parser for strace.;BoIs called for every styled region of the input.<AjGet the parser for styled.==AjGet the parser for svelte.>>AjGet the parser for thrift.??AjGet the parser for turtle.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa60AjGet the parser for unison.1ChThe \xe2\x80\x98vscode\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6ChThe \xe2\x80\x98zephyr\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6Eb(De)serialized as <code>abap-cds</code>, <strong>not</strong> <code>abapcds</code>0Ee(De)serialized as <code>htmlangular</code>, <strong>not</strong> <code>angular</code>0Bf(De)serialized as <code>antlers</code>0Bf(De)serialized as <code>aptconf</code>0Ea(De)serialized as <code>arduino</code>, <strong>not</strong> <code>arduino</code>0DjProvides the <code>arduino</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>aspperl</code>0Bf(De)serialized as <code>aspectj</code>0Bf(De)serialized as <code>authzed</code>0DjProvides the <code>authzed</code> language, supports these file types: \xe2\x80\xa6Eb(De)serialized as <code>avro-idl</code>, <strong>not</strong> <code>avroidl</code>0Bf(De)serialized as <code>befunge</code>0Ea(De)serialized as <code>bitbake</code>, <strong>not</strong> <code>bitbake</code>0DjProvides the <code>bitbake</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>cangjie</code>0DkA capture group with a numeric ID and the contained <code>Pattern</code>\xe2\x80\xa6Bf(De)serialized as <code>catalog</code>0Bf(De)serialized as <code>charity</code>0Bf(De)serialized as <code>chatito</code>0DjProvides the <code>chatito</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>clarion</code>0Ea(De)serialized as <code>clarity</code>, <strong>not</strong> <code>clarity</code>0DjProvides the <code>clarity</code> language, supports these file types: \xe2\x80\xa6Ea(De)serialized as <code>clipper</code>, <strong>not</strong> <code>clipper</code>0Bf(De)serialized as <code>clojure</code>0DjProvides the <code>clojure</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>collada</code>0CdA line comment, including the leading <code>;</code>0Bf(De)serialized as <code>comment</code>0DcProvides the <code>comment</code> language, supports no file types.Dm(De)serialized as <code>ini</code>, <strong>not</strong> <code>confini</code>0Bf(De)serialized as <code>context</code>0Ea(De)serialized as <code>crontab</code>, <strong>not</strong> <code>crontab</code>0Bf(De)serialized as <code>crystal</code>0Bf(De)serialized as <code>cuplsim</code>0Bf(De)serialized as <code>cycript</code>0Bf(De)serialized as <code>daslang</code>0Bf(De)serialized as <code>desktop</code>0DjProvides the <code>desktop</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>dnsmasq</code>0Bf(De)serialized as <code>doxygen</code>0DjProvides the <code>doxygen</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>dracula</code>0Ea(De)serialized as <code>eelixir</code>, <strong>not</strong> <code>eelixir</code>0Bf(De)serialized as <code>epuppet</code>0Bf(De)serialized as <code>eclipse</code>0DjProvides the <code>eelixir</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>elmfilt</code>0Bf(De)serialized as <code>enforce</code>0DjProvides the <code>enforce</code> language, supports these file types: \xe2\x80\xa6CnAn escaped character to match literally (eg. <code>%%</code>).DoAn escaped character to match literally (eg. <code>%%</code>, <code>%]</code>).AdAn escaped characterBf(De)serialized as <code>esmtprc</code>0Bf(De)serialized as <code>esterel</code>0Bf(De)serialized as <code>exports</code>0Ea(De)serialized as <code>figfont</code>, <strong>not</strong> <code>figfont</code>0Bf(De)serialized as <code>focexec</code>0Ea(De)serialized as <code>fortran</code>, <strong>not</strong> <code>fortran</code>0DjProvides the <code>fortran</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>fpcmake</code>0Bf(De)serialized as <code>futhark</code>0Bf(De)serialized as <code>fvwm2m4</code>0Ea(De)serialized as <code>gemtext</code>, <strong>not</strong> <code>gemtext</code>0DjProvides the <code>gitdiff</code> language, supports these file types: \xe2\x80\xa6Ea(De)serialized as <code>gnuplot</code>, <strong>not</strong> <code>gnuplot</code>0DjProvides the <code>gnuplot</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>graphql</code>0DjProvides the <code>graphql</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>hamster</code>0Bf(De)serialized as <code>haproxy</code>0Bf(De)serialized as <code>haskell</code>0DjProvides the <code>haskell</code> language, supports these file types: \xe2\x80\xa600Bf(De)serialized as <code>hexdump</code>0DjProvides the <code>hexdump</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>icemenu</code>0Bf(De)serialized as <code>inittab</code>0Ag<code>^</code> in a setBf(De)serialized as <code>javadoc</code>0DjProvides the <code>javadoc</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>jsonnet</code>0DjProvides the <code>jsonnet</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>kconfig</code>0Ea(De)serialized as <code>kscript</code>, <strong>not</strong> <code>kscript</code>0DjProvides the <code>kconfig</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>labview</code>0Bf(De)serialized as <code>lalrpop</code>0DcProvides the <code>lalrpop</code> language, supports no file types.0Bf(De)serialized as <code>langium</code>0DjMatches any letter; equivalent to <code>[a-zA-Z]</code> (<code>%a</code>).Bf(De)serialized as <code>lidris2</code>0AfA type of log message.Bf(De)serialized as <code>logtalk</code>0Bf(De)serialized as <code>lolcode</code>0Bf(De)serialized as <code>m3build</code>0Bf(De)serialized as <code>m3quake</code>0Bf(De)serialized as <code>mailcap</code>0Bf(De)serialized as <code>mallard</code>0Bf(De)serialized as <code>manconf</code>0Ea(De)serialized as <code>markojs</code>, <strong>not</strong> <code>markojs</code>0Ea(De)serialized as <code>mermaid</code>, <strong>not</strong> <code>mermaid</code>0DjProvides the <code>mermaid</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>modconf</code>0Bf(De)serialized as <code>modsim3</code>0Ea(De)serialized as <code>modula2</code>, <strong>not</strong> <code>modula2</code>0Ea(De)serialized as <code>modula3</code>, <strong>not</strong> <code>modula3</code>0Eb(De)serialized as <code>monkey-c</code>, <strong>not</strong> <code>monkeyc</code>0Bf(De)serialized as <code>moonbit</code>0Bf(De)serialized as <code>mrxvtrc</code>0Bf(De)serialized as <code>natural</code>0Bf(De)serialized as <code>nearley</code>0Bf(De)serialized as <code>nemerle</code>0Bf(De)serialized as <code>netlinx</code>0Bf(De)serialized as <code>netlogo</code>0Ea(De)serialized as <code>objdump</code>, <strong>not</strong> <code>objdump</code>0DjProvides the <code>objdump</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>omgrofl</code>0Bf(De)serialized as <code>openvpn</code>0Bf(De)serialized as <code>oxygene</code>0Bf(De)serialized as <code>pamconf</code>0Bf(De)serialized as <code>papyrus</code>0DeA list of <code>PatternObject</code>s, representing an entire Lua \xe2\x80\xa6Eb(De)serialized as <code>php_only</code>, <strong>not</strong> <code>phponly</code>0DdProvides the <code>php_only</code> language, supports no file types.00Bf(De)serialized as <code>plpgsql</code>0Bf(De)serialized as <code>postcss</code>0Ea(De)serialized as <code>postscr</code>, <strong>not</strong> <code>postscr</code>0Ea(De)serialized as <code>privoxy</code>, <strong>not</strong> <code>privoxy</code>0Bf(De)serialized as <code>problog</code>0DjProvides the <code>problog</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>promela</code>0Eb(De)serialized as <code>ragel-rb</code>, <strong>not</strong> <code>ragelrb</code>0Bf(De)serialized as <code>redcode</code>0Bf(De)serialized as <code>sensors</code>0Bf(De)serialized as <code>sexplib</code>0Bf(De)serialized as <code>slpconf</code>0Bf(De)serialized as <code>smgllnx</code>0Ea(De)serialized as <code>snippet</code>, <strong>not</strong> <code>snippet</code>0Bf(De)serialized as <code>snobol4</code>0Bf(De)serialized as <code>spajson</code>0Bf(De)serialized as <code>specman</code>0Bf(De)serialized as <code>sudoers</code>0Bf(De)serialized as <code>sugarss</code>0Ea(De)serialized as <code>surface</code>, <strong>not</strong> <code>surface</code>0Bf(De)serialized as <code>sxhkdrc</code>0DjProvides the <code>sxhkdrc</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>systemd</code>0Bf(De)serialized as <code>texinfo</code>0Bf(De)serialized as <code>textile</code>0Bf(De)serialized as <code>todotxt</code>0DjProvides the <code>todotxt</code> language, supports these file types: \xe2\x80\xa6Ea(De)serialized as <code>trace32</code>, <strong>not</strong> <code>trace32</code>0DjProvides the <code>trace32</code> language, supports these file types: \xe2\x80\xa6Bf(De)serialized as <code>treetop</code>0Bf(De)serialized as <code>upstart</code>0DjProvides the <code>verilog</code> language, supports these file types: \xe2\x80\xa6Dn(De)serialized as <code>help</code>, <strong>not</strong> <code>vimhelp</code>0Bf(De)serialized as <code>viminfo</code>0Bf(De)serialized as <code>vimhelp</code>0Bf(De)serialized as <code>xmodmap</code>0BjAdvances the iterator to the next element.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AkGet the parser for arduino.11AkGet the parser for authzed.22AkGet the parser for bitbake.3AkGet the parser for c_sharp.004AkGet the parser for chatito.55AkGet the parser for clarity.66AkGet the parser for clojure.77AkGet the parser for comment.8CiThe \xe2\x80\x98darcula\xe2\x80\x99 theme collection in this module was \xe2\x80\xa69AkGet the parser for desktop.::AkGet the parser for doxygen.;CiThe \xe2\x80\x98dracula\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6<AkGet the parser for eelixir.==AkGet the parser for enforce.>CgCreates an iterator which flattens nested streaming \xe2\x80\xa6?AkGet the parser for fortran.Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6CmReturns a mutable reference to the current element of the \xe2\x80\xa61AkGet the parser for gitdiff.22CmGet the parser for gnuplot. (not supported on WebAssembly \xe2\x80\xa633AkGet the parser for graphql.4CiThe \xe2\x80\x98gruvbox\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6AkGet the parser for haskell.006AkGet the parser for hexdump.7CkCall a closure on each element, passing the element on. \xe2\x80\xa6CkChecks if <code>get()</code> will return <code>None</code>.9AkGet the parser for javadoc.::AkGet the parser for jsonnet.;;AkGet the parser for kconfig.<BiGet this node\xe2\x80\x99s type as a numerical id.=AkGet the parser for lalrpop.0CiCreates an iterator which transforms elements of this \xe2\x80\xa6CjIterate over all of the matches in the order that they \xe2\x80\xa6CiThe \xe2\x80\x98melange\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AkGet the parser for mermaid.1CiThe \xe2\x80\x98minimal\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6CiThe \xe2\x80\x98monokai\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6CiThe \xe2\x80\x98moonfly\xe2\x80\x99 theme collection in this module was \xe2\x80\xa6DjConstructs a raw <code>TSQuery</code> pointer without performing extra \xe2\x80\xa6CjIs called at the end of every input line. The returned \xe2\x80\xa66AkGet the parser for objdump.7BePre-load the given list of languages.008AkGet the parser for problog.9DgProcess the given <code>code</code> using the language specified by \xe2\x80\xa6CmProcess a piece of code in the given language, and return \xe2\x80\xa6;AkGet the parser for sxhkdrc.<BjGet an S-expression representing the node.=AkGet the parser for todotxt.>>AkGet the parser for trace32.?Ad<code>verdant</code>Do<strong>This parser is not supported by this parser collection and </strong>\xe2\x80\xa6AkGet the parser for verilog.1Bg(De)serialized as <code>alsaconf</code>0Ec(De)serialized as <code>apkbuild</code>, <strong>not</strong> <code>apkbuild</code>0Bg(De)serialized as <code>asciidoc</code>0Ed(De)serialized as <code>ascii-stl</code>, <strong>not</strong> <code>asciistl</code>0Bg(De)serialized as <code>asterisk</code>0Bg(De)serialized as <code>automake</code>0DfA balanced pattern (eg. <code>%bxy</code>). Matches all characters \xe2\x80\xa6o<code>%b</code>Ec(De)serialized as <code>bindzone</code>, <strong>not</strong> <code>bindzone</code>0Bg(De)serialized as <code>bpftrace</code>0DkProvides the <code>bpftrace</code> language, supports these file types: \xe2\x80\xa6Ed(De)serialized as <code>c-objdump</code>, <strong>not</strong> <code>cobjdump</code>0Bg(De)serialized as <code>calendar</code>0Bg(De)serialized as <code>cameligo</code>0Bg(De)serialized as <code>cfengine</code>0Ec(De)serialized as <code>chaskell</code>, <strong>not</strong> <code>chaskell</code>0Ec(De)serialized as <code>checksum</code>, <strong>not</strong> <code>checksum</code>0Bg(De)serialized as <code>chordpro</code>0EeMatches any control character; equivalent to <code>[\\0-\\31]</code> (<code>%c</code>).Ec(De)serialized as <code>cucumber</code>, <strong>not</strong> <code>cucumber</code>0Ed(De)serialized as <code>d-objdump</code>, <strong>not</strong> <code>dobjdump</code>0Bg(De)serialized as <code>dictconf</code>0Ec(De)serialized as <code>dosbatch</code>, <strong>not</strong> <code>dosbatch</code>0Bg(De)serialized as <code>dylanlid</code>0Bg(De)serialized as <code>euphoria</code>0Bg(De)serialized as <code>execline</code>0CbA color or link with additional style information.0Dn(De)serialized as <code>fsd</code>, <strong>not</strong> <code>facility</code>0BiA non-exhaustive list of text file types.0DkA frontier pattern (eg. <code>%f[a-z]</code>). Matches if the following \xe2\x80\xa6o<code>%f</code>Bg(De)serialized as <code>gdscript</code>0Ec(De)serialized as <code>gdshader</code>, <strong>not</strong> <code>gdshader</code>0DkProvides the <code>gdscript</code> language, supports these file types: \xe2\x80\xa6Ef(De)serialized as <code>gdshaderinc</code>, <strong>not</strong> <code>gdshader</code>0Bg(De)serialized as <code>gitolite</code>0Bg(De)serialized as <code>goaccess</code>0Bg(De)serialized as <code>hercules</code>0Bg(De)serialized as <code>hgcommit</code>0Bg(De)serialized as <code>hostconf</code>0Bg(De)serialized as <code>hyprlang</code>0DkProvides the <code>hyprlang</code> language, supports these file types: \xe2\x80\xa6Bg(De)serialized as <code>i3config</code>0Bg(De)serialized as <code>ipfilter</code>0Bg(De)serialized as <code>isabelle</code>0Ed(De)serialized as <code>jison-lex</code>, <strong>not</strong> <code>jisonlex</code>0Ec(De)serialized as <code>lhaskell</code>, <strong>not</strong> <code>lhaskell</code>0Bg(De)serialized as <code>lambdapi</code>0BdInformation about a loaded language. type identifying a language that is included in this set.CkAn opaque object that defines how to parse a particular \xe2\x80\xa6010Bg(De)serialized as <code>ldapconf</code>0Bg(De)serialized as <code>ligolang</code>0Bg(De)serialized as <code>lilypond</code>0Bg(De)serialized as <code>litestep</code>0Bg(De)serialized as <code>livebook</code>0Bg(De)serialized as <code>logcheck</code>0Ec(De)serialized as <code>markdown</code>, <strong>not</strong> <code>markdown</code>0DkProvides the <code>markdown</code> language, supports these file types: \xe2\x80\xa600Ed(De)serialized as <code>maven-pom</code>, <strong>not</strong> <code>mavenpom</code>0Bg(De)serialized as <code>messages</code>0Bg(De)serialized as <code>minizinc</code>0Bg(De)serialized as <code>mustache</code>0Bg(De)serialized as <code>nextflow</code>0Bg(De)serialized as <code>ntriples</code>0Bg(De)serialized as <code>nwscript</code>0Bg(De)serialized as <code>ocamllex</code>0DkProvides the <code>ocamllex</code> language, supports these file types: \xe2\x80\xa6Bg(De)serialized as <code>omnimark</code>0Bg(De)serialized as <code>openroad</code>0Bg(De)serialized as <code>openscad</code>0Bg(De)serialized as <code>openqasm</code>0Bg(De)serialized as <code>piglatin</code>0Bg(De)serialized as <code>plaintex</code>0Bg(De)serialized as <code>plantuml</code>0Bg(De)serialized as <code>portugol</code>0Bg(De)serialized as <code>procmail</code>0Bg(De)serialized as <code>procfile</code>0Ec(De)serialized as <code>progress</code>, <strong>not</strong> <code>progress</code>0Bg(De)serialized as <code>proguard</code>0Ed(De)serialized as <code>qt-script</code>, <strong>not</strong> <code>qtscript</code>0Bg(De)serialized as <code>quickbms</code>0Ec(De)serialized as <code>radiance</code>, <strong>not</strong> <code>radiance</code>0Bg(De)serialized as <code>rascript</code>0Bg(De)serialized as <code>rescript</code>0Ec(De)serialized as <code>readline</code>, <strong>not</strong> <code>readline</code>0DkProvides the <code>readline</code> language, supports these file types: \xe2\x80\xa6Bg(De)serialized as <code>registry</code>0EgA <code>Renderer</code> defines how to render <code>Highlights</code> to end users. \xe2\x80\xa6DkProvides the <code>rescript</code> language, supports these file types: \xe2\x80\xa6Ec(De)serialized as <code>routeros</code>, <strong>not</strong> <code>routeros</code>0Do(De)serialized as <code>self</code>, <strong>not</strong> <code>selflang</code>0Bg(De)serialized as <code>services</code>0Bg(De)serialized as <code>sgmldecl</code>0Ec(De)serialized as <code>snipmate</code>, <strong>not</strong> <code>snipmate</code>0Bg(De)serialized as <code>solidity</code>0DkProvides the <code>solidity</code> language, supports these file types: \xe2\x80\xa6Ec(De)serialized as <code>solution</code>, <strong>not</strong> <code>solution</code>0Bg(De)serialized as <code>squirrel</code>0DkProvides the <code>squirrel</code> language, supports these file types: \xe2\x80\xa6Ec(De)serialized as <code>starlark</code>, <strong>not</strong> <code>starlark</code>0Bg(De)serialized as <code>swiftgyb</code>0Bg(De)serialized as <code>tablegen</code>0DkProvides the <code>tablegen</code> language, supports these file types: \xe2\x80\xa6Bg(De)serialized as <code>taskdata</code>0Bg(De)serialized as <code>taskedit</code>0Ec(De)serialized as <code>template</code>, <strong>not</strong> <code>template</code>0Bg(De)serialized as <code>teraterm</code>0Bg(De)serialized as <code>terminfo</code>0Bg(De)serialized as <code>textgrid</code>0Bg(De)serialized as <code>tiltfile</code>0Bg(De)serialized as <code>trustees</code>0Ec(De)serialized as <code>typespec</code>, <strong>not</strong> <code>typespec</code>0DkProvides the <code>typespec</code> language, supports these file types: \xe2\x80\xa6Bg(De)serialized as <code>udevconf</code>0Bg(De)serialized as <code>udevperm</code>0Bg(De)serialized as <code>updatedb</code>0Bg(De)serialized as <code>webmacro</code>0Ed(De)serialized as <code>wgsl_bevy</code>, <strong>not</strong> <code>wgslbevy</code>")