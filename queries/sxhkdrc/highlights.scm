;; Forked from https://github.com/nvim-treesitter/nvim-treesitter/blob/master/queries/sxhkdrc/highlights.scm
;; Licensed under the Apache License 2.0
(modifier) @keyword

(operator) @operator

(attribute) @attribute

(command_sync_prefix) @type

(punctuation) @punctuation.bracket

(delimiter) @punctuation.delimiter

(keysym) @variable

(comment) @comment @spell

(range) @number

"\\n" @punctuation.special
